Cycle 4: **0/3 pass** (na 0.31, dian 0.38, heng_zhe 0.49) — avg visual dropped to 0.39 because two brand-new strokes entered. Key finding: a *wrong number* in memory transfers as faithfully as a right one — the Drawer applied the Curator's "na heading ~285°" verbatim, but 285° is nearly vertical; na needs ~325° (a shallow ~45° descent). dian was drawn as a line but is actually a tiny round dot. heng_zhe got the right-angle fold shape correct (closest, 0.49) but was oversized and off-center. Memory now has corrected na heading, a `t.dot()` recipe for dian, and a "compound segments are shorter + recenter" rule. Recommend: carry all three over (na, dian, heng_zhe) — none passed, and each now has a specific fix to test.
