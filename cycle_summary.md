Cycle 6 (first Phase-2 cycle): **2/3 correct** by the is_correct signal — 十 OCR'd as 十 @ 0.78 and 人 as 人 @ 0.95, both composed first-try from scaled heng/shu/pie/na recipes with NO character-specific memory. This is strong evidence that memorized atomic-stroke recipes compose into recognizable characters. The phase transition was the right call: OCR `is_correct` is a far cleaner signal than the noisy phase-correlation visual_score (which sits ~0.15–0.28 even for correct characters now — it should NOT be optimized in Phase 2). Only failure: 一 (single large horizontal) was not recognized — a lone featureless line is an OCR blind spot (same root cause as the Phase-1 thin-stroke metric noise). Memory now has a Phase-2 section: scale strokes ~4-5× & center, prefer a thicker pen (~6-10) for OCR, and treat single-stroke 一 as a known hard case. Recommendation: continue Phase 2 with more multi-stroke simple characters (e.g. 二/三 test the 一 hypothesis, or 大/口/木 add new compositions); either drop 一 or retry it once with a bold near-flat pen to test the thickness hypothesis.
